<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Modelamiento de sistemas</title>
    <%- include('partials/head')%>

    <script type="module" src="/js/calculadora.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/mathjs/12.0.0/math.js"></script>
  </head>
  <body>
    <%if(usuario){%> <%- include('partials/navigation', {usuario:usuario})%>
    <%}else{%> <%- include('partials/navigation')%> <%}%>

    <header
      class="container vh-100 d-flex justify-content-center align-items-center"
    >
      <div class="d-flex flex-column justify-content-center align-items-center">
        <img src="/img/fazt.png" alt="" class="w-25" />
        <h1 class="display-1 my-5 text-uppercase font-weight-bold">
          Esteban Paez
        </h1>
        <a href="/about" class="btn btn-primary w-25">Acerca del proyecto</a>
      </div>
    </header>

    <main class="container">
      <%if(usuario){%>
        <%- include("partials/calculadora", {usuario:usuario})%>
      <%}else{%>
      <div class="d-flex flex-column justify-content-center align-items-center">
        <a href="/crear-cuenta" class="btn btn-primary w-auto"
          >Registrate primero</a
        >
      </div>
      <%}%>
    </main>    
    <%- include('partials/footer')%>
    <script src="https://cdn.jsdelivr.net/npm/chart.js@3.8.0/dist/chart.min.js"></script>
  </body>

</html>
